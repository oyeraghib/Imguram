<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_feed_hot">

    <fragment
        android:id="@+id/navigation_feed_hot"
        android:name="com.example.instagramclone.ui.feed.FeedFragment"
        android:label="@string/title_hot"
        tools:layout="@layout/fragment_feed" >
    </fragment>

    <fragment
        android:id="@+id/navigation_feed_top"
        android:name="com.example.instagramclone.ui.feed.FeedFragment"
        android:label="@string/title_top"
        tools:layout="@layout/fragment_feed" >
    </fragment>

</navigation>